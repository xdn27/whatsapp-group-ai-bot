
# --- LLM Provider ---
# Choose between 'openai' or 'openrouter'
LLM_PROVIDER=openai

# --- OpenAI Configuration ---
OPENAI_API_KEY=
OPENAI_MODEL=gpt-3.5-turbo

# --- OpenRouter Configuration ---
# Get your API key from https://openrouter.ai/keys
OPENROUTER_API_KEY=
# Example models: 'mistralai/mistral-7b-instruct', 'google/gemini-pro', 'anthropic/claude-3-haiku'
OPENROUTER_MODEL=mistralai/mistral-7b-instruct
# Your website or app URL is required by the OpenRouter API
OPENROUTER_REFERER=https://your-app.com

# --- Bot Personality ---
BOT_LANGUAGE=id
BOT_STYLE=santai
BOT_PERSONA=seorang teman yang membantu
BOT_NOTE=

# --- Group Filtering ---
# Comma-separated list of WhatsApp Group NAMES (e.g., 'My Awesome Group,Another Chat')
# If left empty, bot will respond in all groups.
ALLOWED_GROUPS=

# --- Trigger Keywords ---
# Comma-separated list of keywords that will trigger the bot to respond even if not mentioned.
# Example: 'bot,ask,help'
# If left empty, bot will only respond when mentioned.
TRIGGER_KEYWORDS=

# --- Mention Trigger Keywords ---
# Comma-separated list of keywords that will trigger the bot to respond ONLY if mentioned.
# Example: 'question,help,query'
# If left empty, bot will respond to any mention.
MENTION_TRIGGER_KEYWORDS=

# --- Database ---
DATABASE_URL="file:./dev.db"

# --- Content Moderation ---
CONTENT_MODERATION_ENABLED=true
MODERATION_PROMPT="You are a content moderator for a WhatsApp group. Your task is to analyze the following message and determine if it contains pornography, profanity, hate speech, or other inappropriate topics. Answer ONLY with 'YES' if the message is inappropriate, and 'NO' if it is appropriate. Message to analyze: '{MESSAGE_CONTENT}'"
MODERATION_WARNING_PROMPT="You are an AI assistant tasked with keeping a WhatsApp group atmosphere positive. An inappropriate message was just detected. Create a short, polite, and firm warning message to remind all members to maintain communication ethics in the group. Do not quote the inappropriate message. Just write your warning message."
